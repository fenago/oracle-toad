create cluster clus_deptno (deptno number(2))
storage (initial 10k next 10K PCTINCREASE 0)
pctfree 50
pctused 30;

CREATE  INDEX DEPTNO_INDX
ON CLUSTER CLUS_DEPTNO;

CREATE TABLE NEWDEPT
CLUSTER CLUS_DEPTNO (DEPTNO)
AS SELECT * FROM DEPT;

CREATE TABLE NEWEMP
CLUSTER CLUS_DEPTNO (DEPTNO)
AS SELECT * FROM EMP;

ALTER TABLE DEPT
PCTFREE 50
PCTUSED 30;
ALTER TABLE EMP
PCTFREE 50
PCTUSED 30;
